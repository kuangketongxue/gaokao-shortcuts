# 统一换行符：仓库内一律 LF，避免 Windows 上 checkout 后 CRLF 造成
# 索引哈希漂移、CI --check 误报
* text=auto eol=lf

# 二进制/字体不走文本转换
*.woff2 binary
*.png binary
*.jpg binary
*.ico binary

# 生成文件在 GitHub 语言统计中不计入
site/vendor/** linguist-vendored
